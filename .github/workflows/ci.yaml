#name: ci-golang-workflow
#on: [push]
#jobs:
#  check-application:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-go@v2
#        with:
#          go-version: 1.18
#      - run: go test
#      - run: go run math.go

# name: ci-golang-workflow
# on: 
#   pull_request:
#     branches:
#       - develop
# jobs:
#  check-application:
#    runs-on: ubuntu-latest
#    strategy:
#     matrix:
#       go: ['1.17', '1.18']
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-go@v2
#        with:
#          go-version: ${{ matrix.go }}
#      - run: go test ./CI_class
#      - run: go run ./CI_class/math.go

#      - name: Set up QEMU
#        uses: docker/setup-qemu-action@v1

#      - name: Set up docker buildx
#        uses: docker/setup-buildx-action@v1

#      - name: Login to dockerhub
#        uses: docker/login-action@v1
#        with:
#         username: ${{secrets.DOCKERHUB_USERNAME}}
#         password: ${{secrets.DOCKERHUB_TOKEN}}

#      - name: Build and push
#        id: docker_build
#        uses: docker/build-push-action@v6
#        with:
#          push: false
#          tags: gabrielggs/full-cycle:latest

# name: ci-sonarcloud
# on:
#   pull_request:
#     branches:
#       - develop

# jobs:
#   run-ci:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: actions/setup-go@v2
#         with:
#           go-version: 1.15
#       - run: go test ./CI_class -coverprofile=coverage.out

# ---

name: CD Gitops
on:
  push:
    branches:
      - develop
    paths:
      - git_ops/**
      - .github/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Build and push image to Dockerhub
        uses: docker/build-push-action@v1.1.0
        with:
          username: ${{secrets.DOCKERHUB_USERNAME}}
          password: ${{secrets.DOCKERHUB_TOKEN}}
          repository: ${{secrets.DOCKERHUB_USERNAME}}/full-cycle
          tags: ${{github.sha}}, latest
          path: ./git_ops

      - name: Setup kustomize
        uses: imranismail/setup-kustomize@v1
        with:
          kustomize-version: "3.6.1"

      - name: Update kubernetes resources
        env:
          DOCKER_USERNAME: ${{secrets.DOCKERHUB_USERNAME}}
        run: 
          cd k8s
          kustomize edit set image goserver=$DOCKER_USERNAME/full-cycle:$GITHUB_SHA

      - name: Commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git commit -am "Bump docker version"

      - name: Push
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          repository: GSousaGabriel/full-cycle